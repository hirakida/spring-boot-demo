spring:
  datasource:
    sql-script-encoding: UTF-8  # SQLファイルの文字コード
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:h2:file:/tmp/h2_jpa_db2
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate  # DDLの実行をJPAではなくFlywayで実行する
