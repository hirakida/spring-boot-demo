spring:
  datasource:
    url: jdbc:h2:mem:batch_tasklet_db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  batch:
    job:
      enabled: false
  output:
    ansi:
      enabled: always
