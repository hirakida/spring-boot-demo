spring:
  cloud:
    stream:
      bindings:
        input:
          destination: topic1
  devtools:
    livereload:
      enabled: false
  output:
    ansi:
      enabled: always
server:
  port: 8081
