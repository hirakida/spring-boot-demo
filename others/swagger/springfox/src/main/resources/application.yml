spring:
  datasource:
    sql-script-encoding: UTF-8
    url: jdbc:h2:mem:swagger-db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  output:
    ansi:
      enabled: always
