spring:
  output:
    ansi:
      enabled: always
  cache:
    type: simple
logging:
  level:
    org.springframework.cache: trace
management:
  endpoints:
    web:
      exposure:
        include: ["health", "info", "caches"]
