spring:
  output:
    ansi:
      enabled: always
  cache:
    type: redis
#    cache-names: default
#    redis:
#      time-to-live: 60s
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
logging:
  level:
    org.springframework.cache: trace
