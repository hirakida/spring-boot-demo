spring:
  output:
    ansi:
      enabled: always
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    pool:
      max-active: 8
      max-idle: 8
      min-idle: 0
logging:
  level:
    org.springframework.cache: trace
