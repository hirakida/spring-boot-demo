spring:
  cache:
    type: redis
#    cache-names: cache1
#    redis:
#      time-to-live: 60s
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
  output:
    ansi:
      enabled: always
