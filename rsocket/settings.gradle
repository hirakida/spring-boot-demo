pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'rsocket-demo'
include 'frontend-server',
        'tcp-server',
        'websocket-server',
        'model'
