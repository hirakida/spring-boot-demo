spring:
  datasource:
    url: jdbc:h2:mem:graphql_db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  graphql:
    schema:
      printer:
        enabled: true
  output:
    ansi:
      enabled: always
