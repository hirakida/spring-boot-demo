spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: sa
    password: pass
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
  output:
    ansi:
      enabled: always
