spring:
  datasource:
    url: jdbc:h2:mem:jpa-db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
  output:
    ansi:
      enabled: always
logging:
  level:
    org.springframework.orm.jpa.JpaTransactionManager: DEBUG
