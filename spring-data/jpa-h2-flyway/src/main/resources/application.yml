spring:
  datasource:
    sql-script-encoding: UTF-8
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:h2:file:/tmp/h2_jpa_flyway_db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate  # DDLの実行をJPAではなくFlywayで実行する
