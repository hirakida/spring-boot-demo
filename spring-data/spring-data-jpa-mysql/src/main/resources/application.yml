spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql-db
    username: user
    password: passwd
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  output:
    ansi:
      enabled: always
