spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres-db
    username: sa
    password: passwd
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  output:
    ansi:
      enabled: always
