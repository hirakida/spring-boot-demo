spring:
  web:
    resources:
      chain:
        strategy:
          content:
            enabled: true
#      cache:
#        cachecontrol:
#          max-age: 1h
  output:
    ansi:
      enabled: always
logging:
  level:
    org.springframework.web.servlet.mvc.WebContentInterceptor: trace
