spring:
  datasource:
    url: jdbc:h2:mem:aop-db;NON_KEYWORDS=USER
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  output:
    ansi:
      enabled: always
