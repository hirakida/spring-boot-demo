spring:
  web:
    resources:
      chain:
        strategy:
          content:
            enabled: true
  output:
    ansi:
      enabled: always
server:
  error:
    include-exception: true
    include-message: always
