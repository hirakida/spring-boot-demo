spring:
  output:
    ansi:
      enabled: always
management:
  metrics:
    export:
      prometheus:
        pushgateway:
          enabled: true
          base-url: http://localhost:9091
          job: pushgateway-demo
          shutdown-operation: push
