spring:
  session:
    store-type: mongodb
  output:
    ansi:
      enabled: always
server:
  servlet:
    session:
      cookie:
        max-age: 60s
        name: SPRING_SESSION_MONGODB
      timeout: 60s
