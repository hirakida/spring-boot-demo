spring:
  datasource:
    url: jdbc:h2:file:/tmp/flyway_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  output:
    ansi:
      enabled: always
  flyway:
    baseline-on-migrate: true
logging:
  level:
    org.flywaydb.core: debug
