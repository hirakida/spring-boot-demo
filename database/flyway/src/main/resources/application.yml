spring:
  datasource:
    sql-script-encoding: UTF-8
    url: jdbc:h2:file:/tmp/flyway_h2_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  output:
    ansi:
      enabled: always
