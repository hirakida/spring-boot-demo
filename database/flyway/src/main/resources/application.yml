spring:
  datasource:
    url: jdbc:h2:file:./db/flyway_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
#  flyway:
#    baseline-on-migrate: true
#    baseline-version: 1.0.0
  output:
    ansi:
      enabled: always
logging:
  level:
    org.flywaydb.core: debug
