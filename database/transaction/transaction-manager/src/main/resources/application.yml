spring:
  datasource:
    url: jdbc:h2:mem:transaction_db
    username: sa
    password:
  output:
    ansi:
      enabled: always
logging:
  level:
    org.springframework.jdbc.datasource.DataSourceTransactionManager: DEBUG
