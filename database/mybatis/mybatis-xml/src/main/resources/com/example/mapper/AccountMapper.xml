<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AccountMapper">
  <sql id="selectAll">
    SELECT `id`, `name`, `created_at`, `updated_at` FROM `account`
  </sql>

  <select id="findAll" resultType="com.example.entity.Account">
    <include refid="selectAll"></include>
  </select>

  <select id="findById" parameterType="long" resultType="com.example.entity.Account">
    <include refid="selectAll"></include>
    WHERE `id` = #{id}
  </select>

  <select id="findByIds" parameterType="java.util.List" resultType="com.example.entity.Account">
    <include refid="selectAll"></include>
    WHERE id IN
    <foreach item="id" index="index" collection="list" open="(" separator="," close=")">
      #{id}
    </foreach>
  </select>
</mapper>
