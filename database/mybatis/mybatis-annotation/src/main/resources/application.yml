spring:
  datasource:
    sql-script-encoding: UTF-8
    url: jdbc:h2:mem:/mybatis-db
    username: sa
    password:
  devtools:
    livereload:
      enabled: false
  output:
    ansi:
      enabled: always
mybatis:
  configuration:
    map-underscore-to-camel-case: true
