rootProject.name = 'spring-boot-actuator-demo'

include 'actuator-custom-endpoints',
        'actuator-health',
        'actuator-info',
        'actuator-jmx',
        'actuator-prometheus',
        'actuator-prometheus-pushgateway',
        'actuator-startup'
